#ifndef INTRO_HPP
#define INTRO_HPP

#include <QWidget>

class intro : public QWidget
{
    Q_OBJECT
public:
    explicit intro(QWidget *parent = nullptr);
    ~intro();
signals:

public slots:
};

#endif // INTRO_HPP
